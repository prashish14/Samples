using System;
using Autofac;

namespace Sample.Application.WebApi
{
    public class WebApiModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            //builder.RegisterModule<BLLModule>();
            //builder.RegisterType<SecurityContextService>().As<ISecurityContext>().InstancePerRequest();
        }
    }
}